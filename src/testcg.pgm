integer function myfunction()
  integer i;
  integer a[5];
  integer function arfunc(integer b)
   begin
    arfunc := b;
   end function;
begin
  i := 1;
  a[0] := 0;
  while (i < 5)
    a[i] := a[i-1] + 5;
    i := i + 1;
  end while;
  myfunction := arfunc(3);
end function
