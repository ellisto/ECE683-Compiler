integer function myfunction()
  integer arr[5];
  integer arr2[5];
  integer i;
  integer tmp;
begin
  i :=0;
    while (i < 5)
    arr[i] := i;
    tmp := putInt(arr[i]);
    i := i + 1;
  end while;
  tmp := putString(" ");
  arr2 := arr;
  myfunction := putInt(arr2[3]);
end function
